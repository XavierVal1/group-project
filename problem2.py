# Understand - Take an integer and turn it into the factorial of that integer
# Clues - factorial function?
# Assemble - import math module to use math functions, take integer input from user, print the factorial of that integer
# Solve
# Examine



import math
num = int(input("input a number: "))
factorialNum = math.factorial(num)
print(factorialNum)